/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 */
import 'zone.js'; // Included with Angular CLI.

/***************************************************************************************************
 * Add any additional polyfills below. For Angular Elements, include the custom-elements polyfill if
 * you need to support legacy browsers (e.g. IE11 or older Edge).
 */

// If you want legacy browser support for custom elements (OPTIONAL, modern browsers do NOT need this):
import '@webcomponents/custom-elements';